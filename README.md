# Kreiraj README s ispravnim formatom
cat > README.md << 'EOF'
# Conway's Game of Life - Common Lisp Implementation

## How to Run
```bash
# Clone repository
git clone https://github.com/jukiclarija/conway-game-of-life-lisp.git
cd conway-game-of-life-lisp

# Run the game
sbcl --load game-of-life.lisp --quit# Conway's Game of Life - Common Lisp Implementation

## 📋 Project Description
Implementation of Conway's Game of Life using Common Lisp and functional programming paradigm for the Declarative Programming course.

## 🚀 Installation & Usage

### Requirements
- Ubuntu/Linux
- SBCL (Steel Bank Common Lisp)

### Installation
```bash
# Install SBCL
sudo apt update
sudo apt install sbcl

# Clone repository
git clone https://github.com/jukiclarija/conway-game-of-life-lisp.git
cd conway-game-of-life-lisp
